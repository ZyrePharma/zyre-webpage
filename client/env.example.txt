# SMTP Email Configuration
# Configure these values for your SMTP server

# SMTP Server Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-app-password

# Email Sender Information
SMTP_FROM_EMAIL=noreply@zyrepharma.com
SMTP_FROM_NAME=Zyre Pharma Careers

# Notification Recipient
NOTIFICATION_EMAIL=hr@zyrepharma.com

# Strapi Configuration (if not already set)
# STRAPI_API_KEY=your-strapi-api-key
